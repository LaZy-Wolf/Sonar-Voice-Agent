name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      # setup-uv already creates .venv and exports VIRTUAL_ENV; --system would hit
      # the runner's externally managed Python, and `uv venv` would hit the existing one.
      - run: echo "$PWD/.venv/bin" >> "$GITHUB_PATH"
      - run: uv pip install -e "./mcp-server[dev]" -e "./agent[dev]"
      - run: ruff check .
      - run: pytest mcp-server/tests agent/tests -q

  # The web/ job is added in stage 6, when web/ exists. A job guarded on
  # hashFiles('web/package.json') does not work: hashFiles is unavailable in a
  # job-level `if`, and the whole workflow fails to parse.
